import type { ReactNode } from "react";
import "@/styles/classic-tokens.css";
import "@/styles/classic-ai-bridge.css";
export default function AIStudyLayout({children}:{children:ReactNode}){return <div className="c-ai-zone">{children}</div>;}
